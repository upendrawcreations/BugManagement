package com.cineflow.bug.repository; import com.cineflow.bug.entity.BugFrame; import java.util.*; import org.springframework.data.jpa.repository.JpaRepository; public interface BugFrameRepository extends JpaRepository<BugFrame,UUID>{ List<BugFrame> findAllByBugIdOrderByCreatedAt(UUID bugId); }
